import sys

sys.stdin = open("_Flatten.txt")

for i in range(1,11):
    dump = int(input())

    box = list(map(int, input().split()))

    while dump:
        max_box = max(box)
        min_box = 
############################################################

    print('#{} {}'.format())